#include<stdio.h>
main()
{
	int a, i, br = 0;
	for (i = 1; i <= 7; i++) {
		printf("Ucitaj broj:");
		scanf("%d", &a);
		if (a > 10 && a < 20)
			br++;
	}
	printf("Takvih je brojeva %d", br);
}
